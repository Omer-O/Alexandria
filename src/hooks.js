function updateBstatus() {
    const [friendshipStatus, setStatus] = useState(currentStatus);

    useEffect( 	setStatus(newStatus);

                    }
                )
                });
        },
        [friendshipStatus]
    );
}
